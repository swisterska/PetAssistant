<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/background"
    android:padding="10dp">


    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:padding="9dp">

        <!-- ImageButton -->
        <ImageButton
            android:id="@+id/GoBackButtonHistory"
            android:layout_width="55dp"
            android:layout_height="55dp"
            android:background="#00FFFFFF"
            android:src="@drawable/gobackcimpleblack"
            android:layout_marginEnd="10dp"/>

        <!-- Empty space to allow TextView to take available space -->
        <Space
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"/>

    </LinearLayout>

    <TextView
        android:id="@+id/FoodTimesText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ellipsize="none"
        android:fontFamily="@font/glikerblackfont"
        android:gravity="center"
        android:scrollHorizontally="false"
        android:singleLine="false"
        android:text="Check your history"
        android:textColor="@color/black"
        android:textSize="25sp"
        android:translationY="70dp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center">

        <ImageButton
            android:id="@+id/SymptomsHistory"
            android:layout_width="200dp"
            android:layout_height="65dp"
            android:background="#00FFFFFF"
            android:src="@drawable/symptomshistory"
            android:contentDescription="Symptoms History" />
    </LinearLayout>



</RelativeLayout>